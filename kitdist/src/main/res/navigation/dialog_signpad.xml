<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/signpad_dialog"
    app:startDestination="@id/flow_signpad_dialog_dest"
    tools:ignore="UnusedNavigation">

    <dialog
        android:id="@+id/flow_signpad_dialog_dest"
        android:name="kr.co.kit.cordova.plugin.signpad.SignPadDialogFragment"
        android:label="@string/fragment_dialog_label_signpad"
        tools:layout="@layout/fragment_dialog_signpad" />
</navigation>
